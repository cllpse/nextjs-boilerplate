import { Color } from "@new/Color"

export type ProgressIndicatorItemSegmentProps = {
  color: Color
  width: `${number}%`
  label?: string
}

export const ProgressIndicatorSegment = ({
  // eslint-disable-next-line
  color,

  // eslint-disable-next-line
  width,

  // eslint-disable-next-line
  label,
}: ProgressIndicatorItemSegmentProps) => null
