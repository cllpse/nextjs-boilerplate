import { Color } from "@new/Color"

export type ProgressIndicatorItemTickProps = {
  color: Color
  label: string
  offset: `${number}%`
}

export const ProgressIndicatorTick = ({
  // eslint-disable-next-line
  color,

  // eslint-disable-next-line
  offset,
}: ProgressIndicatorItemTickProps) => null
