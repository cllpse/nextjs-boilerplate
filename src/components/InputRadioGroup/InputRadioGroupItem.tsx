import { ComponentBaseProps } from "@new/ComponentBaseProps"

export type InputRadioGroupItemProps = ComponentBaseProps & {
  value: string
  label: string
}

export const InputRadioGroupItem = ({
  // eslint-disable-next-line
  value,
  // eslint-disable-next-line
  label,
}: InputRadioGroupItemProps) => null
